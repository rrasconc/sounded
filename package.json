{
  "name": "sounded",
  "version": "1.0.0",
  "description": "Guess the song's name game",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.mjs",
    "start": "node index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm install && npm run build --output client"
  },
  "author": "rigobertorascon",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "node-fetch": "^3.2.6"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
